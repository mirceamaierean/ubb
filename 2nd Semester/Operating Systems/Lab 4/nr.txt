a8213
1) grep -E '\b[A-Z]\w*\b' nr.txt
2) sed -E 's/([0-9])([0-9])/\2\1/g' nr.txt
3) awk '{print $1 + $2}' nr.txt  
4) sort nr.txt | uniq -u
5) for file in *.txt; do
  # Check if the file contains the word "cat"
  if grep -q "cat" "$file"; then
    # If the word "cat" is found, display the file name
    echo "$file"
  fi
done